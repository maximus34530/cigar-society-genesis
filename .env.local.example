# Local-only example for enabling GitHub MCP.
# Copy to `.env.local` and set your real token locally (do NOT commit).

GITHUB_PERSONAL_ACCESS_TOKEN=YOUR_GITHUB_PERSONAL_ACCESS_TOKEN

# Non-secret example values.
# Set these in your local environment (and restart Cursor if needed) before using GitHub MCP.

GITHUB_PERSONAL_ACCESS_TOKEN=YOUR_GITHUB_PERSONAL_ACCESS_TOKEN

